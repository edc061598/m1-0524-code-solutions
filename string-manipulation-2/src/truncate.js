"use strict";
/* exported truncate */
function truncate(length, string) {
    const newString = "";
    for (let index = 0; index < string.length; index++) {
        string[index];
    }
    return newString;
}
